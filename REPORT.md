# 施設個別ページ SEO実装｜10施設パイロット 報告書

対象: おでかけナビ（https://odekakenavi.github.io/odekake-navi/）
フェーズ: Google流入強化のための施設個別ページ、10施設のみ先行実装
600施設への一括展開: **未実施**（今回はここまで）

---

## 前提として分かったこと（実装前の調査結果）

着手前にコードを調査したところ、**個別URL・SEOタグ・構造化データの「クライアント側の仕組み」は既に実装済み**でした。

- `SLUG_OVERRIDES`（施設名→確定slug、668件登録済み）
- `spotSlug()` / `spotBySlug()`（施設⇄URLの相互変換）
- `updateSeoTagsForSpot()`（施設を開くたびに title・meta description・canonical・OGP・BreadcrumbList・TouristAttractionの構造化データを書き換え）
- `sitemap.xml` には、既に668件中633件の個別施設URL・全11都道府県のハブページ・季節ページが登録済み
- GitHub Pages の404.htmlリダイレクト（"spa-github-pages" 手法）で、`/tokyo/taito/ueno/` のような直接アクセスも `index.html` に復元される

一方、コード内コメントに以下の記載があり、**実ファイルとして存在する静的HTML（＝クローラーがJSを実行せずとも読める本物のページ）を作る `tools/build-static-pages.js` は、施設データが `data/spots.json` に外部化されて以降、動かしていない**状態でした。

> 静的ページ生成ツール（tools/build-static-pages.js）は、以前はこのファイル内の SPOTS〜spotBySlug を文字列で取り出していた。施設データを data/spots.json へ移したため、ツール側を「data/spots.json を読む」形に直すまで実行しないこと

つまり今回のタスクの実体は「ゼロから作る」のではなく、**「data/spots.json 対応版の静的ページ生成ツールを作り直し、10施設分を実際に生成する」**ことでした。この方針で実装しています。既存の index.html（クライアント側ルーティング）はコードを一切変更していません＝既存機能への影響ゼロです。

---

## ① 10施設のURL一覧

既存の `SLUG_OVERRIDES` に登録済みのslugをそのまま使用（新しいURL体系は作らず、既存構造に準拠）。10件とも既に `sitemap.xml` に登録済みだったURLです。

| # | 施設名 | カテゴリ | URL |
|---|---|---|---|
| 1 | 上野動物園 | 動物園 | https://odekakenavi.github.io/odekake-navi/tokyo/taito/ueno/ |
| 2 | すみだ水族館 | 水族館 | https://odekakenavi.github.io/odekake-navi/tokyo/sumida/sumida-aquarium/ |
| 3 | よみうりランド | 遊園地・テーマパーク | https://odekakenavi.github.io/odekake-navi/tokyo/inagi/yomiuriland/ |
| 4 | 国営昭和記念公園 | 大型公園 | https://odekakenavi.github.io/odekake-navi/tokyo/tachikawa/showakinen-koen/ |
| 5 | はまぎん こども宇宙科学館 | 屋内施設（雨の日OK） | https://odekakenavi.github.io/odekake-navi/kanagawa/yokohama-isogo/hamagin-space-science-center/ |
| 6 | 鉄道博物館 | 博物館・科学館 | https://odekakenavi.github.io/odekake-navi/saitama/saitama-city/railway-museum/ |
| 7 | キッザニア東京 | 商業施設・屋内遊び場 | https://odekakenavi.github.io/odekake-navi/tokyo/koto/kidzania/ |
| 8 | アンパンマンこどもミュージアム＆モール横浜 | 子ども向け人気施設 | https://odekakenavi.github.io/odekake-navi/kanagawa/yokohama-nishi/yokohama-anpanman/ |
| 9 | 東京都水の科学館 | 無料・低料金施設 | https://odekakenavi.github.io/odekake-navi/tokyo/koto/mizunokagaku/ |
| 10 | 国営ひたち海浜公園 | 神奈川県外の代表施設 | https://odekakenavi.github.io/odekake-navi/ibaraki/hitachinaka/hitachikaihin/ |

**選定について**: サイト全体のデータは神奈川県が最多（177件）ですが、今回は「テンプレートの完成度を検証する」ことが主目的のため、データが豊富で知名度も高い施設を優先しました。神奈川県の施設は2件（#5, #8）のみです。地域バランスは次フェーズ以降の改善点として⑪に記載しています。

---

## ② 各施設のtitle

`index.html` の `updateSeoTagsForSpot()` と全く同じ組み立てロジック（`${施設名}｜子連れお出かけ情報｜おでかけナビ`）を使用し、クライアント版と静的版でtitleが食い違わないようにしています。

1. 上野動物園｜子連れお出かけ情報｜おでかけナビ
2. すみだ水族館｜子連れお出かけ情報｜おでかけナビ
3. よみうりランド｜子連れお出かけ情報｜おでかけナビ
4. 国営昭和記念公園｜子連れお出かけ情報｜おでかけナビ
5. はまぎん こども宇宙科学館｜子連れお出かけ情報｜おでかけナビ
6. 鉄道博物館｜子連れお出かけ情報｜おでかけナビ
7. キッザニア東京｜子連れお出かけ情報｜おでかけナビ
8. アンパンマンこどもミュージアム＆モール横浜｜子連れお出かけ情報｜おでかけナビ
9. 東京都水の科学館｜子連れお出かけ情報｜おでかけナビ
10. 国営ひたち海浜公園｜子連れお出かけ情報｜おでかけナビ

---

## ③ 各施設のdescription

同じく `buildSpotMetaDescription()` と同一ロジック（`${施設名}（${都道府県+エリア}）の子連れお出かけ情報。` + `desc`、120文字超は省略）で自動生成。**施設名だけ差し替えた同一文章ではなく、各施設の `desc` フィールド（実データ）に基づき内容が変わります。**

1. 上野動物園（東京都台東区・上野公園）の子連れお出かけ情報。パンダ目当てで混みやすいが、園内は広く動物の種類も豊富。ベビーカーで回れる坂道多め。
2. すみだ水族館（東京都墨田区・東京スカイツリータウン）の子連れお出かけ情報。スカイツリー直結。照明が落ち着いていて、人混みが苦手な乳幼児連れでも過ごしやすい。
3. よみうりランド（東京都稲城市・川崎市にまたがる）の子連れお出かけ情報。未就学児向けの緩やかな乗り物から絶叫系まで揃う遊園地。プールやイルミも季節ごとに展開。
4. 国営昭和記念公園（東京都立川市）の子連れお出かけ情報。広大な芝生広場、じゃぶじゃぶ池、大型遊具のある「みんなの原っぱ」まで揃う総合公園。春は約20万球のチューリップ…
5. はまぎん こども宇宙科学館（神奈川県横浜市磯子区）の子連れお出かけ情報。科学と宇宙をテーマにした体験型展示とプラネタリウムが楽しめる施設。
6. 鉄道博物館（埼玉県さいたま市大宮）の子連れお出かけ情報。実物車両の展示に加え、運転シミュレータや模型ジオラマも充実。電車好きなら1日いける。
7. キッザニア東京（東京都江東区・豊洲）の子連れお出かけ情報。約100種のお仕事・社会体験ができる屋内型施設。時間入れ替え制で1日がっつり滞在するタイプ。
8. アンパンマンこどもミュージアム＆モール横浜（神奈川県横浜市西区）の子連れお出かけ情報。未就学児にターゲットを絞った屋内施設。低い年齢でも安全に遊べる作り。
9. 東京都水の科学館（東京都江東区・有明）の子連れお出かけ情報。水をテーマにした無料の体験型施設。館内は空調完備で雨の日・猛暑日の避難先にも便利。
10. 国営ひたち海浜公園（茨城県ひたちなか市）の子連れお出かけ情報。ネモフィラやコキアで有名な丘陵の公園。大型遊具エリアや自転車コースもある。

---

## ④ 各施設ページに表示される項目

`data/spots.json` に実在するフィールドのみを表示し、値が無い項目はセクションごと非表示にしています（「情報なし」の羅列はしていません）。施設によって表示されるセクション数は異なります（例：上野動物園は「営業時間」表示あり、キッザニア東京は `businessHours` が未登録のため非表示）。

- 施設名（h1）／カテゴリ・地域バッジ
- パンくずリスト（おでかけナビ › 都道府県 › 市区町村 › 施設名）
- おすすめポイント（`desc` + `tip`）
- 対象年齢の目安（`ages`。「0〜1歳」等、既存の年齢帯表記に統一）
- 基本情報（所要時間・天候との相性・料金・駐車場・ベビーカー/授乳室・アクセス・食事）
- 営業時間（`businessHours` がある施設のみ、曜日別テーブル）＋定休日
- 混雑の目安（`congestion` がある施設のみ、平日/土日祝 × 午前/午後）
- 現在地からの距離（ボタン押下でブラウザのGeolocation APIを使い、施設の緯度経度との直線距離を計算。位置情報の取得は任意・クリック起点のみで、ページ読み込み時には要求しません）
- 公式サイトへのリンク／情報の最終確認日（`verifiedAt` がある施設のみ）
- チケット・予約情報（`bookingLinks` がある施設のみ。外部予約サイトへの遷移は `rel="nofollow noopener"`）
- アプリ版で開くボタン（`?spot=施設名` で index.html の該当施設詳細を直接開く。既存の共有URL機構をそのまま利用）
- 周辺のお出かけスポット（実際にURLが存在する施設のみ、半径15km以内・近い順に最大4件）
- 周辺のファミリー向けホテル（`data/hotels.json` から半径10km以内・近い順に最大3件。個別ホテルページはまだ存在しないためリンクはせず、トップページのホテル検索へ誘導）
- 関連ページ（トップ・都道府県ページ・市区町村ページ・該当する場合は季節ページ）

---

## ⑤ 内部リンク構造

```
トップページ
 └─ 都道府県ページ（例: /tokyo/）
     └─ 市区町村ページ（例: /tokyo/taito/）
         └─ 施設ページ（例: /tokyo/taito/ueno/）←今回作成
             ├─→ 周辺のお出かけスポット（最大4件、実在URLのみ）
             ├─→ 季節ページ（seasonsフィールドがある施設のみ。例: よみうりランド→夏・冬）
             ├─→ アプリ版（トップページを ?spot= 付きで開く）
             └─→ トップ／都道府県／市区町村ページへ戻るリンク
```

「目的ページ」「今日どこ行く？」は、現状クローラブルなURLとして存在しない（アプリ内シートのみ）ため、**存在しないページへのリンクは作成していません**。将来これらのページ化が完了した時点で内部リンクを追加する想定です。

---

## ⑥ 構造化データ

`index.html` の `buildSpotJsonLd()` / `setBreadcrumbJsonLdForSpot()` と同一方針（データに無い項目は省略、画面表示内容と一致させる）で、`BreadcrumbList` と `TouristAttraction` の2種類をJSON-LDで出力しています。上野動物園の例：

```json
{
  "@context": "https://schema.org",
  "@type": "TouristAttraction",
  "name": "上野動物園",
  "url": "https://odekakenavi.github.io/odekake-navi/tokyo/taito/ueno/",
  "description": "パンダ目当てで混みやすいが、園内は広く動物の種類も豊富。ベビーカーで回れる坂道多め。",
  "address": { "@type": "PostalAddress", "addressRegion": "東京都", "addressLocality": "台東区", "addressCountry": "JP" },
  "geo": { "@type": "GeoCoordinates", "latitude": 35.716, "longitude": 139.7712 },
  "sameAs": "https://www.tokyo-zoo.net/zoo/ueno/"
}
```

町域までの正式な住所（`streetAddress`）はデータに存在しないため含めていません（詳細は⑪）。全10件、`https://validator.schema.org/` 相当のJSON構文チェックは通過しています（本環境はネットワーク接続がないため、実際のGoogle Rich Results Testでの検証は未実施です。デプロイ後にご確認ください）。

---

## ⑦ sitemapへの追加内容

10施設とも、**既に `sitemap.xml` に登録済みだった**ため新規追加はありません（`SLUG_OVERRIDES` 登録済み施設は既存の仕組みで自動的にsitemap化されていたため）。今回は、実データに `verifiedAt`（情報確認日）がある7件について `<lastmod>` タグを追加しました。

- 既存URL数: 904件 → 変更後: 904件（**削除・追加ともにゼロ**、既存URLを一切壊していません）
- `<lastmod>` を追加した7件: 上野動物園・すみだ水族館・よみうりランド・国営昭和記念公園・はまぎんこども宇宙科学館・鉄道博物館・東京都水の科学館（いずれも2026-09-16）
- `<lastmod>` を追加していない3件（`verifiedAt` が未登録のため）: キッザニア東京・アンパンマンこどもミュージアム＆モール横浜・国営ひたち海浜公園

将来600施設へ展開する際は、`verifiedAt` がある施設から機械的に `<lastmod>` を埋められる構造です。

---

## ⑧ robots.txtへの影響

**変更していません。** 既存の `robots.txt` は `Allow: /` で全パスを許可しており、今回追加した10ページのパス（例: `/tokyo/taito/ueno/`）も既に許可範囲内です。sitemapの参照先も変更していません。

---

## ⑨ 既存機能への影響

- **index.html は一切編集していません。** 検索・絞り込み・お気に入り・行った記録・プラン・比較・今日どこ行く？・現在地検索・郵便番号検索・近い順・施設詳細表示・外部リンク等、既存の全機能はコード変更ゼロにつき影響ありません。
- **アプリ内でのページ遷移（クリックでの施設詳細表示）にも影響ありません。** アプリ内は `history.pushState` によるクライアント側遷移のため、今回追加した実ファイルへのHTTPリクエストは発生しません。
- **挙動が変わる点が1つあります**: これまで `/tokyo/taito/ueno/` 等のURLに直接アクセス（またはリロード）すると、404.htmlのリダイレクト経由で「アプリ本体が起動し、該当施設の詳細シートが開いた状態」が表示されていました。今回、この10件のパスに実ファイルを配置したため、直接アクセス・リロード時は「今回作成した軽量な静的ページ」が表示されるようになります（アプリ本体は読み込まれません）。静的ページ内の「アプリ版で開く」ボタンから、これまで通りの詳細シート表示にワンタップで遷移できます。この10件以外の633件（自動生成されるURLを含めると668件）は従来通りの挙動のままです。
- スマートフォン表示は、アプリ本体と同じCSS変数（配色・フォント）を使い、375px幅を想定したレイアウトで確認しています（実機での見た目チェックは、ネットワーク接続のない本環境では未実施のため、公開後に一度ご確認をお願いします）。

---

## ⑩ 600施設へ展開する際に必要な作業

1. `tools/build-static-pages.js` の `PILOT_FACILITY_NAMES`（10件の配列）を、`SLUG_OVERRIDES` の全キー（現在668件）に差し替える。
2. `node tools/build-static-pages.js` を実行する（このツールは data/spots.json・data/hotels.json・index.html の `SLUG_OVERRIDES` を読むだけで、他のファイルには一切書き込みません）。
3. 生成された668件分の `{slug}/index.html` をコミット・デプロイする。
4. `sitemap.xml` はほぼ全件が既に登録済みのため追加作業は不要。`verifiedAt` がある施設分だけ `<lastmod>` を追記する処理を、今回のsitemap更新スクリプトを流用して自動化する。
5. 600件規模になると生成ファイル数も668件になるため、GitHub Pagesのビルド・デプロイ時間、リポジトリのファイル数増加（1施設1ディレクトリ）を事前に確認しておく。
6. 「周辺のお出かけスポット」「周辺ホテル」は施設ごとに動的計算のため、データ更新（`data/spots.json` / `data/hotels.json` の編集）のたびに `build-static-pages.js` を再実行し、静的ページを再生成する運用が必要になる（＝この静的ページは「ビルド成果物」として扱い、手編集しない）。

---

## ⑪ 今回の10施設で分かった改善点

- **地域バランス**: 選定した10施設は東京都が6件、神奈川県が2件、埼玉県・茨城県が各1件。サイトの主要データが神奈川県（177件、全体の最多）であることを踏まえると、次のバッチでは神奈川県の代表施設（新江ノ島水族館・よこはま動物園ズーラシアなど）をもっと厚めに含めるのが良さそうです。
- **住所（`streetAddress`）データが存在しない**: 現在の `data/spots.json` は `region`（都道府県）・`area`（市区町村＋地区名）・`lat`/`lng` は持っていますが、丁目・番地までの正式な住所フィールドがありません。構造化データの `PostalAddress` も市区町村止まりになっています。今後、正式な番地情報を追加できると、Googleマップ連携や構造化データの精度がさらに上がります。
- **ホテルの個別URLが未整備**: `data/hotels.json` の各ホテルは `slug` フィールド（例: `hotel/yamanashi/hotel-mt-fuji`）を既に持っていますが、対応する個別ページ・sitemap登録はまだ無いため、今回は「周辺ホテル」をテキスト情報として掲載するに留めました。ホテル個別ページ化も将来のSEO拡張候補です。
- **`businessHours`／`congestion`／`stayTime` のカバレッジがまだ部分的**: 722施設中、`businessHours` は33件、`congestion` は43件、`stayTime` は185件のみ登録済みです。個別ページの情報量・検索意図への対応度は、これらのフィールドの拡充に比例して上がるため、600施設展開の優先順位づけに「データが豊富な施設から」という軸を入れるのも一案です。
- **画像が一切ない**: `data/spots.json` に画像URLのフィールドが無いため、今回の10ページはテキストと絵文字のみの構成です。Google画像検索やSNSシェア時のサムネイル（OGP）は、サイト共通の `og-image.png` を暫定的に使っています。施設ごとの画像データが用意できれば、クリック率の面でも改善余地があります。
- **静的ページと本体アプリの見た目の差**: 今回のページはあえて軽量（favoriteボタン等の機能を持たない）にしましたが、「お気に入り登録」「行った記録」ボタンをこの時点で静的ページに直接持たせるかどうかは検討の余地があります（今回は「アプリ版で開く」への誘導のみに留めています）。

---

## 動作確認について（環境上の制約）

本環境はネットワーク接続が無効化されているため、以下は**実施できていません**。デプロイ後に確認をお願いします。

- 実際のブラウザでの表示崩れチェック（HTML/CSSの静的検証と、既存サイトと同一のCSS変数・フォント指定の踏襲は確認済みです）
- Google Rich Results Test / Search Console でのインデックス状況・構造化データの検証
- 実URLでの404.html経由の直接アクセス確認

---

# 追記：フェーズ2（10施設 → 100施設への拡張）

## 選定方法（90施設を追加）

前段の10施設は手作業でカテゴリを分散させて選びましたが、**今後600施設まで拡張することを見据え、フェーズ2からは再現性のある機械的な選定ロジックに切り替えました**。`tools/build-static-pages.js` は対象施設リストを `tools/facility-rollout-list.json`（施設名の配列）から読み込む方式に変更し、この配列を差し替えるだけで対象を増減できるようにしています。

選定ロジック（100施設）:

1. `SLUG_OVERRIDES` に確定URLが登録済み・かつ `data/spots.json` に実データがある668施設を母集団とする。
2. 都道府県ごとの母集団の比率に応じて、100件の配分数を按分（最大剰余方式）。神奈川146件→22件、東京119件→18件、埼玉82件→12件…という形で、母集団が多い都道府県ほど多く選ばれるようにしています。
3. 元の10施設は据え置き、各都道府県の配分数から「その都道府県で既に選ばれている件数」を差し引いた残り枠を、`businessHours`・`congestion`・`stayTime`・`closedDays`/`holidays`・`bookingLinks`・`verifiedAt`・`seasons`・`genre` の充実度が高い施設から順に埋めました（データが薄い施設を先に量産しない、という⑪の改善点に対応）。

結果の都道府県別内訳（100件）:

| 都道府県 | 件数 |
|---|---|
| 神奈川 | 22 |
| 東京 | 18 |
| 埼玉 | 12 |
| 千葉 | 12 |
| 栃木 | 8 |
| 群馬 | 8 |
| 茨城 | 7 |
| 静岡 | 6 |
| 山梨 | 4 |
| 長野 | 3 |
| 福島 | 0（母集団2件のみのため按分上0件。次フェーズ以降で個別に含める想定） |

※フェーズ1で指摘した「神奈川県の割合が少ない」という改善点は、按分方式にしたことでこのフェーズから解消しています。

## sitemap.xmlへの影響（フェーズ2）

100施設のうち **98件は既にsitemap.xmlに登録済み**でした。未登録だった以下の2件を新規追加しています（他は追加・削除ともになし、既存904件はそのまま）。

- `https://odekakenavi.github.io/odekake-navi/kanagawa/yokohama-tsurumi/bay-bridge-skywalk/`（横浜ベイブリッジスカイウォーク）
- `https://odekakenavi.github.io/odekake-navi/chiba/urayasu/tokyodisneysea/`（東京ディズニーシー）

また、`verifiedAt`（情報確認日）が登録されている施設について、フェーズ1・2を通じて合計35件に `<lastmod>` を設定しました。sitemap.xmlの総URL数は 904件 → 906件（追加2、削除0）です。

※「東京ディズニーシー」については、掲載内容はサイト自身が保有する事実情報（料金・所要時間・アクセス等）のみで、キャラクター名や商標画像・第三者コンテンツの転載は含まれていません。一般的な旅行ガイド記事が観光地名として言及するのと同じ扱いです。

## 生成結果

- 生成ファイル数: **100件**（`{slug}/index.html`、対象100件すべて成功、失敗0件）
- 生成方法: `node tools/build-static-pages.js`（`tools/facility-rollout-list.json` を読み込んで実行。ロジック・テンプレートはフェーズ1と完全に同一で、titleやdescriptionの重複生成・存在しない情報の記載は発生していません）
- 既存機能への影響: フェーズ1と同様、`index.html` は無変更のため影響なし。直接アクセス時の挙動変化（静的ページが表示される）が、今回追加した90施設のURLにも同様に適用されます。

## 600施設への展開に向けて（更新）

`tools/facility-rollout-list.json` を `SLUG_OVERRIDES` の全668キーに差し替えて `node tools/build-static-pages.js` を実行するだけで、残り568施設分も同じ品質・同じロジックで生成できる状態になっています。次にやるべきことは、これまでの報告どおり以下の3点です。

1. `facility-rollout-list.json` を全件リストに差し替える
2. 生成・デプロイ後、Search Consoleでインデックス状況を確認する
3. 住所（丁目・番地）データ、ホテル個別ページなど、⑪で挙げたデータ拡充を並行して進める

---

# 追記：フェーズ3（100施設 → 200施設への拡張）

## 選定方法

フェーズ2と同じロジックで、**目標件数を200件に引き上げて再計算**しました（668件の母集団に対する都道府県比率を200件ベースで按分し、既に選ばれている100件を各都道府県の割当から差し引いた残り枠を、データの充実度が高い順に埋める）。目標件数ベースで按分し直しているため、フェーズ2までの100件の内訳自体は変更していません（据え置き）。

200件時点の都道府県別内訳:

| 都道府県 | フェーズ2まで(100件) | フェーズ3で追加 | 累計(200件) |
|---|---|---|---|
| 神奈川 | 22 | 22 | 44 |
| 東京 | 18 | 18 | 36 |
| 埼玉 | 12 | 12 | 24 |
| 千葉 | 12 | 12 | 24 |
| 栃木 | 8 | 8 | 16 |
| 群馬 | 8 | 8 | 16 |
| 茨城 | 7 | 7 | 14 |
| 静岡 | 6 | 6 | 12 |
| 山梨 | 4 | 3 | 7 |
| 長野 | 3 | 3 | 6 |
| 福島 | 0 | 1 | 1 |

福島県は母集団が2件のみですが、200件規模になったことで按分上はじめて1件（アクアマリンふくしま）が対象に入りました。

## 生成結果

- 生成ファイル数: **200件**（対象200件すべて成功、失敗0件）
- `tools/facility-rollout-list.json` は累計200件（フェーズ2の100件 + フェーズ3で追加した100件）に更新済み。次回さらに拡張する場合も、このファイルを更新して同じコマンドを再実行するだけです。

## sitemap.xmlへの影響

フェーズ3で追加した100件のうち、**99件は既にsitemap.xmlに登録済み**でした。未登録だった以下の1件のみ新規追加しています。

- `https://odekakenavi.github.io/odekake-navi/gunma/isesaki/kezoji-yuenchi/`（華蔵寺公園遊園地）

`verifiedAt` がある施設への `<lastmod>` 付与は、フェーズ3で新たに13件追加しました（フェーズ1〜3累計で48件）。sitemap.xmlの総URL数は 904件 → **907件**（フェーズ1〜3累計で追加3件、削除0件）です。

## 現在の到達点（フェーズ3終了時点）

- 静的ページ生成済み: 200 / 668施設（登録済み母集団に対して約30%）
- 668施設すべての `SLUG_OVERRIDES` 未登録分（722 − 668 = 54施設）は、まずURLのslug確定（`SLUG_OVERRIDES`への追加）が必要なため、今回の対象には含めていません。次フェーズで静的ページ化を進める場合は、先にこの54施設分のslug登録作業が必要です。

---

# 追記：フェーズ4（200施設 → SLUG_OVERRIDES登録済み全668施設が完了）

## 対応内容

ここまでは按分＋データ充実度でサンプリングしていましたが、今回は「残り468施設」すべて（＝`SLUG_OVERRIDES` に登録済みの668施設のうち、まだ生成していなかった全件）を対象にしました。選定のための優先順位付けは行わず、単純に「まだ作っていない登録済み施設は全部作る」という方針です。

- 対象: 468施設（668 − これまでの200施設）
- 結果: **468件すべて生成成功、失敗0件**
- 累計: **668 / 668施設（SLUG_OVERRIDESに登録済みの施設は100%完了）**

## sitemap.xmlへの影響

468件のうち、**436件は既にsitemap.xmlに登録済み**でした。未登録だった **32件を新規追加**しています（一覧は `tools/pilot-report.json` で全件確認できます。代表例: 羽田空港、三島スカイウォーク、猿島、ボーネルンドあそびのせかい各店舗、リトルプラネット各店舗など、主に商業施設内の遊び場・比較的新しく登録された施設群でした）。

`verifiedAt` がある施設への `<lastmod>` 付与は、今回新たに58件追加しました（フェーズ1〜4累計で106件）。

**sitemap.xmlの総URL数: 904件 → 939件（フェーズ1〜4累計で追加35件、削除0件）。既存URLは一切削除していません。**

## データ品質に関する注意点（1件）

生成中に、`SLUG_OVERRIDES` に **ローマ字化されていないslugが1件だけ**存在することが分かりました。

```
"仙元山見晴らしの丘公園": "saitama/比企郡小川町/sengenyama-miharashi-no-oka"
```

他の667件はすべて `pref/city-romaji/slug-romaji` の形式でローマ字化されていますが、この1件だけ市区町村部分が日本語（`比企郡小川町`）のままです。これは今回作成したデータではなく、**index.html に既存のデータ**です。URLとしては動作しますが（UTF-8のパスとしてブラウザ・GitHub Pagesとも扱えます）、サイト全体の命名規則から外れるため、他の667件と同じ形式（例: `hiki-ogawa`）にローマ字化しておくことを推奨します。値を独自に推測して修正することはせず、**元のデータのまま**ページを生成しています。修正が必要であればお知らせください。

## 現在の到達点（最終）

- **静的ページ生成済み: 668 / 668施設（SLUG_OVERRIDES登録済み分は100%完了）**
- `data/spots.json` 全722施設のうち、`SLUG_OVERRIDES` 未登録の **54施設** は今回の対象に含まれていません。これらはURL（slug）がまだ確定していないため、静的ページを作る前に、まず `index.html` 側で `SLUG_OVERRIDES` にエントリを追加する（＝どのURLで公開するか決める）作業が必要です。この54施設のリストが必要であれば別途お出しできます。
- sitemap.xml: 904件 → 939件（追加35件、削除0件）
- robots.txtへの変更は引き続きなし（既存の `Allow: /` で全パス許可済み）
- `index.html` は全フェーズを通じて一切変更していないため、既存機能（検索・お気に入り・行った記録・プラン・比較など）への影響はありません

## 全668件の一括検証で見つけたバグ（修正済み）

668件すべてを対象に「`undefined`・`[object Object]`の混入」「titleタグの有無」「JSON-LDの構文」を機械的にチェックしたところ、2件で不具合が見つかりました。

- 対象: 東京ディズニーシー・東京ディズニーランド（2施設のみ）
- 原因: `data/spots.json` の `parkingDetail` が、他施設では文字列型なのに対しこの2件だけオブジェクト型（`{note, priceLabel, ratingLabel, ...}`）で入っており、テンプレート側がオブジェクトをそのまま文字列化していたため「駐車場：あり（[object Object]）」のように表示されてしまっていました。
- 対応: `build-static-pages.js` に `formatParkingDetail()` を追加し、文字列・オブジェクトのどちらの形でも正しく表示されるよう修正。668件を再生成し、再チェックで**問題ゼロ**を確認済みです（添付の668件zip・build-static-pages.jsは修正反映済みです）。




